---
id: tags
category: motion
drillable: false
difficulty: advanced
frequency: low
related_drills: []
related_articles:
  - marks-and-jumps
  - quickfix
help_tags:
  - ":h tags"
  - ":h CTRL-]"
  - ":h tagsrch.txt"
---

# Tags — symbol-based navigation

Jump to function and class definitions across files using a pre-built **tags file** (typically generated by `ctags` or `universal-ctags`). Predates LSP by over two decades and still works on any language with a parser.

## Commands

| Key                 | Action                                                      |
|---------------------|-------------------------------------------------------------|
| `<C-]>`             | Jump to definition of word under cursor                     |
| `g<C-]>`            | Same as `:tjump` (prompt on ambiguity)                      |
| `<C-t>`             | Pop back through tag stack                                  |
| `:tag {name}`       | Jump to tag `{name}`                                        |
| `:tag /{pat}`       | Jump to first tag matching pattern                          |
| `:tselect`          | Choose from list of matching tags                           |
| `:tjump`            | Like `:tag` but `:tselect` on ambiguity                     |
| `:tnext` `:tprev`   | Cycle through tag matches                                   |
| `:trewind` `:tlast` | First / last tag match                                      |
| `:tags`             | Show tag stack                                              |
| `:ptag {name}`      | Open tag in **preview window** (split)                      |
| `:pclose`           | Close preview window                                        |

## Choosing between

- **`<C-]>` vs `gd` vs `gD`**: `<C-]>` uses the tags file (full project, all files indexed). `gd` searches within the **current function** for the first declaration-like occurrence (uses `[[` to find function start, falls back to line 1). `gD` searches the **whole file** from line 1. Use `gd` for function-local variables, `gD` for file-wide lookup, `<C-]>` for cross-file symbols.
- **`<C-]>` vs LSP go-to-definition**: tags are language-agnostic, fast, simple — but stale unless you rebuild. LSP is precise, semantic, live — but requires a server per language. Modern setups often use LSP; legacy / multi-language / quick setups still use tags.
- **`:tag` vs `:tjump`**: `:tag` jumps to the first match silently. `:tjump` prompts when ambiguous. Use `:tjump` (or `g<C-]>`) when you don't trust the first hit.
- **`:tag` vs `:ptag`**: `:tag` replaces your buffer. `:ptag` opens a preview split — keeps your current location visible. Use `:ptag` to peek without losing place.
- **Tags vs `:grep`**: Tags are an indexed lookup (fast, definitions only). `:grep` searches text in all files (slow, finds usage and definitions). Use grep for "where is this called from".

## How tags work

1. Generate a `tags` file at project root: `ctags -R .`
2. Vim looks for `tags` via `'tags'` option. **Vim default: `./tags,tags`** (no upward search). **Neovim default: `./tags;,tags`** — the trailing `;` enables upward directory walking, so a project root `tags` file is found from a subdirectory. (`./TAGS,TAGS` is **not** in the default; add it with `set tags+=./TAGS,TAGS` if you use ETAGS-style files.)
3. Press `<C-]>` on a symbol — Vim looks it up, jumps to the file/line listed in `tags`

The format is plain text:
```
funcName    src/foo.c    /^void funcName(...)/
```

## Examples

```text
Workflow:
  $ ctags -R .           # generate tags
  $ vim src/main.c
  /usage_of_foo<CR>      # find a call
  <C-]>                  # jump to definition
  read, edit             # ...
  <C-t>                  # return

Peek without leaving:    :ptag funcName
Choose match:            :tselect funcName
Search by pattern:       :tag /^my_prefix
```

## Pitfalls

- The `tags` file becomes **stale** when code changes. Regenerate (manual `ctags -R` or via `autocmd BufWritePost`).
- `<C-]>` requires the cursor to be **on** a tag word. Punctuation breaks the match.
- Tag lookup case sensitivity follows `'tagcase'` (default `followic`, which means "follow `'ignorecase'`"). Since `'ignorecase'` defaults to off, the practical result is case-sensitive — but the direct default is `followic`, not "case-sensitive".
- For monorepos with thousands of files, generation can be slow — `universal-ctags` with `--exclude` patterns helps.
- LSP and tags can coexist; many users map `<C-]>` to LSP definition and keep tags as fallback.

## See also

- 📖 Related: [marks-and-jumps], [quickfix]
- 📚 `:h tags`, `:h CTRL-]`, `:h tagsrch.txt`, `:h 'tags'`
